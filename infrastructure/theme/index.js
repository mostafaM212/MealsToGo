import { colors } from "./colors";
import { fonts, fontSizes, fontsWeight } from "./fonts";
import { sizes } from "./sizes";
import { lineHeights, space } from "./spacing";


export const theme = {
    colors,
    fonts,
    fontSizes,
    fontsWeight,
    sizes,
    lineHeights,
    space
};